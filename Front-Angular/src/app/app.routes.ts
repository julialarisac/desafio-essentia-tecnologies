import { Routes } from '@angular/router';
import { Lista } from './module/lista-de-tarefas/pages/lista/lista';

export const routes: Routes = [
    {
        path: '',
        component: Lista,
    }
];
